import {Entity} from "../entity/Entity";

/** holds analytical
 *  data for the user (amount of sessions, how many sessions per day, linear progress, ...)
 */
export class UserReportEntity implements Entity {

}